import sched, time, datetime
import requests
import copy
import beepy

import script

DELAY_IN_MINUTES = 0

# loop = sched.scheduler(time.time, time.sleep)
# loop.enter(DELAY_IN_MINUTES * 60, 1, main.main, (loop,))
# loop.run()

print("minu" in "minutes.")